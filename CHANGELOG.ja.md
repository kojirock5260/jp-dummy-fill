# 変更履歴

[English](CHANGELOG.md)

## 1.0.0 — 2026-09-17

### 追加

- クレジットカードの欄。番号（Stripe の Visa テスト番号 4242 4242 4242 4242。欄が区切りを示せば
  4 桁ずつ、4 分割にも対応）、ローマ字の名義、有効期限（MM/YY、MM/YYYY、YY/MM、`type=month`、
  月と年の select）、セキュリティコード、カード会社の select / radio（VISA）
- 法人番号（国税庁の検査用数字が通る 13 桁）と、適格請求書の登録番号（T ＋ 法人番号）
- ローマ字氏名。「氏名（ローマ字）」`name_en` `last_name_en` など。既定は「名 姓」を大文字で、
  欄の例に小文字や姓先があればそれに従う
- 希望日（配達希望日・予約日・来店日）。1 週間後の平日
- パレットに「支払い」のまとまり
- placeholder の形を手掛かりにする。`@username` はユーザー名、`taro@example.com` はメール、
  `https://facebook.com` は URL で、そのドメインの下にユーザー名を付ける
  （`https://facebook.com/abe_shou_0`）。label と placeholder が英語だけの欄には
  「テスト入力」ではなく英語を入れる

### 変更

- バージョン 1.0.0。0.1.0 のルール・データ・UI はそのまま
- `npm run pack` でストア用の zip を作る。Edge 用はショートカットの既定を `Alt+Shift+Y / U / K` に
  する。`Ctrl+Shift+Y / U / K` は Edge 本体が使っているため

## 0.1.0 — 2026-09-05

### 追加

- 日本語フォームの空いている欄を、整合の取れた架空の一人ぶんで埋める。氏名とカナ、
  郵便番号、都道府県、住所とその読み、電話と FAX、メールとパスワード（確認欄も）、
  西暦・和暦の生年月日、年齢、性別、会社・部署・役職、URL、自由記述、同意の checkbox
- 入口は 3 つ。ツールバーのアイコン、3 項目の右クリックメニュー（入力・住所を替える・
  この欄にデータを埋める）、ショートカット `Cmd+Shift+Y`（入力）`Cmd+Shift+U`（住所）
  `Cmd+Shift+K`（パレット）
- 「住所を遠隔地・離島に替える…」は、配送業者が区分している場所（北海道・九州・沖縄と、
  佐川急便の離島中継料一覧から選んだ離島 18）を並べたパレットを開き、選んだ場所で住所の
  欄だけを上書きする。人物は変えない。埋まったページでも効く。以前の「同じ人を遠隔地で
  入力」は空欄しか埋めなかったので、埋まったページでは何も起きなかった
- 右クリックメニューの「この欄にデータを埋める…」と、フォーカスのある欄での `Cmd+Shift+K`。欄のそばに
  パレットが開き、数文字打って絞り込んで Enter で、同じ人物の値を上書きで入れる。規則が外した
  欄の逃げ道。パレットには規則の判定も出て、選んだ欄はコンソールに残る
- 人物はページを開いた時刻から決まり、会員登録を繰り返してもメールアドレスが前回と
  被らない（アドレスに番号が付く）。番号はメールアドレスの末尾に見え、パレットの
  「人物を番号で呼び出す…」（数字を打つだけでもよい）で番号から人を呼び戻せる。
  「固定データで入力」は置かない。同じ人物が要るのは登録したアカウントに入り直すとき
  くらいで、それは番号で足りる
- ページで既に選ばれている都道府県があれば、その県の住所にする
- 欄の判定は `autocomplete`、`type`、`name` / `id`、label、placeholder、select の
  選択肢から。日本語の label を主要なシグナルとして扱う
- 分割欄は番号（`zip1` / `zip2`）、語（`tel_area`）、並び（`tel` が 3 つ連続）、
  `maxlength` で見分ける
- カナの文字種は label・placeholder・name・`pattern` に従う。数字は「全角」の指示と
  `inputmode`、ハイフンは `maxlength` と placeholder に従う
- 郵便番号を先に書いて 300ms 待ち、残りを書く。ページ側の郵便番号→住所補完に住所を
  埋めさせ、町域で止まっていれば番地を足す
- 値はプロトタイプの native setter で入れ、`input` `change` `blur` で知らせる。
  React / Vue の制御コンポーネントでも拾われる。キーイベントは投げないので、
  ふりがな自動入力を誤作動させない
- 実在の郵便番号と町域の組 65 件（中心 47・離島 18）を読み付きで同梱。
  `npm run data` で日本郵便のデータから作り直せる
- 英語と日本語の UI
- 実サイト 7 件（楽天、EC-CUBE 4、LoGoフォーム、Google フォーム、Marketo、
  Snow Monkey Forms、Contact Form 7）のフォームで判定を確かめ、`tests/fixtures/forms/`
  に固定した。そこから直したこと：`<th>` の見出しを label に数える、「氏」は姓、
  「（姓）」は括弧を外して読む、placeholder の「例：5300001」や label の「ハイフン無し」で
  ハイフンの有無を決める、placeholder が「市区町村」「番地」と言えばずれた `autocomplete`
  より優先する、text 欄の `autocomplete="new-password"` は自動入力を止める細工なので
  パスワードにしない、Contact Form 7 の `acceptance` は同意、サイト内検索のフォームと
  value が URL の select は触らない、自由記述系の欄種が select に当たれば先頭の選択肢
  （詳細は `docs/forms-survey.md`）
- Amazon の住所追加（市区町村 / 丁目・番地・号 / 建物名 / 部屋番号 の 4 分割）から、
  欄種 `block` `room` `country` を足した。番地だけの欄があれば市区町村の欄に町名まで、
  部屋番号の欄があれば建物の欄に建物名だけを入れる。国の select は日本を当て、
  当たらなければ触らない。`PostalCodeOne` / `Two` も分割として読む
